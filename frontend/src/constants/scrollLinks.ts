import * as routes from "./routes";

const links = {
  HOME: {
    to: routes.HOME_ROUTE,
    text: "HOME",
  },
  EXPERIENCES: {
    to: routes.EXPERIENCES_ROUTE,
    text: "EXPERIENCES",
  },
};

export default links;
